# Canonical Python deps (Render native, Docker, and docs). Keep backend/requirements.txt in sync (it -r's this file).
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
requests>=2.31.0
python-dotenv>=1.0.0
python-multipart>=0.0.9
pillow>=10.0.0
fal-client>=0.8.0
